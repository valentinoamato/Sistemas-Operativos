# Sistemas Operativos Práctica 2

## System Calls

## Conceptos generales

### 1. ¿Qué es una System Call? ¿Para qué se utiliza?

Una System Call es el mecanismo utilizado por un proceso de usuario para solicitar un servicio al sistema operativo. Los procesos de usuario están limitados a su propio espacio de direcciones y no poseen acceso directo al hardware. Para acceder a funciones o servicios protegidos por el sistema operativo, el proceso en modo usuario realiza una llamada al sistema, la cual puede invocar un proceso para solicitar cierto servicio al SO.

El SO provee una API que los procesos pueden utilizar para solicitar recursos gestionados por él mismo. Las llamadas al sistema están definidas con un conjunto de parámetros concretos que se deben enviar para solicitar los servicios y los valores que se retornan.

### 2. ¿Para qué sirve la macro syscall? Describa el propósito de cada uno de sus parámetros. Ayuda: http://www.gnu.org/software/libc/manual/html_mono/libc.html#System-Calls

Existen tanto una función llamada `syscall` como una macro `_syscall`.

La función `syscall` realiza una llamada al sistema genérica. Esta tiene la forma `long int syscall(long int sysno, ...)`, donde `sysno` es el número de la system call. Cada llamada al sistema se identifica con un número. Los argumentos restantes serán aquellos que la system call que se invoca recibe, por lo que pueden variar dependiendo de la llamada que se efectúe. En caso de indicar más argumentos de los que la system call recibe, los restantes serán ignorados.
La función retorna un valor que corresponde al retorno de la system call, exceptuando que esta haya fallado. En dicho caso retornará -1.
Esta es una función de alto nivel de la `libc`.

La macro `_syscallX` invoca una system call sin soporte de la librería `libc`. Se debe conocer el prototipo de la system call a invocar, tal como cuantos argumentos recibe, sus tipos y el tipo de retorno. Tiene la forma `_syscallX(type,name,type1,arg1,type2,arg2,...)` donde la X posee un valor entre 0-6 y representa el número de argumentos que recibe la system call. `type` es el tipo de retorno y `name` es el nombre de la system call.
Esta macro permite generar funciones wrappers para las system calls que se invocan.
Esta macro es de bajo nivel pero se encuentra en desuso en nuevas versiones.

### 3. Ejecute el siguiente comando e identifique el propósito de cada uno de los archivos que encuentra: `ls -lh /boot | grep vmlinuz`

Aparecen las distintas versiones de `vmlinuz`. Este es la imagen del kernel comprimida `vmlinux`, para bootear el sistema operativo. El archivo resultante de la compresión contiene los elementos esenciales del kernel. Su tamaño reducido permite optimizar la eficiencia y el uso de memoria durante el inicio del sistema.

El archivo original (`vmlinux`) es la imagen del kernel sin comprimir. Esta no puede ser booteada. Contiene el código completo del kernel. Suele ser usado para desarrollar y debuggear el kernel.

### 4. Acceda al codigo fuente de GNU Linux, sea visitando https://kernel.org/ o bien trayendo el código del kernel (cuidado, como todo software monolítico son unos cuantos gigas) (`git clone https://github.com/torvalds/linux.git`)

### 5. ¿Para qué sirve el siguiente archivo? `arch/x86/entry/syscalls/syscall_64.tbl`

El archivo `arch/x86/entry/syscalls/syscall_64.tbl` corresponde a la tabla de system call para la arquitectura x86 de 64 bits. Esta tabla es una estructura de datos del kernel que mapea números de system calls a su correspondiente función en el kernel. Cada system call tiene un número único. Cuando un proceso de usuario invoca una system call, pasa este número al kernel. El kernel utiliza la tabla para encontrar la correspondiente función y ejecutarla.

El formato de las entradas de la tabla es: `<number> <abi> <name> <entry point> [<compat entry point> [noreturn]]`

- `number`: número que identifica la system call
- `abi`: Application Binary Interface, especifica la interfaz binaria y convención para invocar que sigue la system call (puediendo ser common, 64 o 32).
- `name`: nombre de la system call
- `entry point`: función del kernel vinculada a la system call 

### 6. ¿Para qué sirve la herramienta strace? ¿Cómo se usa?

`strace` es una herramienta para el diagnóstico, debugging y monitoreo de procesos en Linux, lo que incluye system calls invocadas, envío de señales y cambios en el estado del proceso. Se utiliza principalmente para debuggear programas, interceptar system calls y monitorear procesos activos. Permite obtener información acerca de como un programa interactúa con el sistema.
Resulta especialmente útil para solucionar problemas de programas para los cuales no se tiene el código fuente

Concretamente para lo concerniente a esta práctica, interesa su utilidad para rastrar system calls.

`strace` ejecuta un comando especificado hasta que este termina. Durante su ejecución, intercepta y registra todas las system calls invocadas por un proceso y las señales que recibe. Imprime el nombre de cada system call, sus argumentos y su valor de retorno.

Su forma de uso es `strace [flag] command [args]`. Por ejemplo, `strace -e trace=write ls` intercepta la system call `write` cuando es invocada por el comando `ls`.

### 7. ¿Para qué sirve la herramienta ausyscall? ¿Cómo se usa?

`ausyscall` es una herramienta que permite mapear nombres de system calls con números. Dada una arquitectura, el programa imprime el mapeo del nombre de una system call indicada a su número correspodiente o viceversa (indicando el número). Se usa de la forma: `ausyscall [arch] name | number | --dump | --exact`. `--dump` imprime la tabla entera, mientras que  `--exact` indica que se busca el string exacto y no coincidencias con substrings. Por ejemplo: `usyscall $(uname -m) write --exact ` devuelve `1` mientras que `usyscall $(uname -m) 1` devuelve `write`.

___

## Práctica guiada

```c
#include <linux/kernel.h>
#include <linux/syscalls.h>
#include <linux/sched.h>
#include <linux/uaccess.h>
#include <linux/sched/signal.h>
#include <linux/slab.h> // Para kmalloc y kfree

SYSCALL_DEFINE1(my_sys_call, int, arg) {
        printk(KERN_INFO "My syscall called with arg: %d\n", arg);
        return 0;
}

SYSCALL_DEFINE2(get_task_info, char __user *, buffer, size_t, length) {
        struct task_struct *task;
        char kbuffer[1024]; // Buffer en el espacio del kernel
        int offset = 0;

        for_each_process(task) {
                offset += snprintf(kbuffer + offset, sizeof(kbuffer) - offset,
                "PID: %d | Nombre: %s | Estado: %d \n",task->pid, task->comm, task_state_index(task));
                if (offset >= sizeof(kbuffer)) // Evita sobrepasar el tamaño del buffer
                        break;

                printk(KERN_INFO "PID: %d | Nombre: %s\n", task->pid, task->comm);
        }

        // Copia la información al espacio de usuario
        if (copy_to_user(buffer, kbuffer, min(length, (size_t)offset)))
                return -EFAULT;
        return min(length, (size_t)offset);
}

SYSCALL_DEFINE2(get_threads_info, char __user *, buffer, size_t, length) {
        struct task_struct *task, *thread;
        char *kbuffer;
        int offset = 0;

        // Asignar memoria dinámica para el buffer
        kbuffer = kmalloc(2048, GFP_KERNEL);
        if (!kbuffer)
                return -ENOMEM;
        for_each_process(task) {
                offset += snprintf(kbuffer + offset, 2048 - offset, "Proceso: %s (PID: %d)\n", task->comm, task->pid);

                for_each_thread(task, thread) {
                        offset += snprintf(kbuffer + offset, 2048 - offset, "    ├── Hilo: %s (TID: %d)\n", thread->comm, thread->pid);

                        if (offset >= 2048)
                                break;
                }

                if (offset >= 2048)
                        break;
        }

        if (copy_to_user(buffer, kbuffer, min(length, (size_t)offset))) {
                kfree(kbuffer);
                return -EFAULT;
        }

        kfree(kbuffer);
        return min(length, (size_t)offset);
}
```

### Mirando el código anterior, investigue y responda lo siguiente?

**- ¿Para qué sirven los macros SYS_CALL_DEFINE?**

La macro permite definir el cuerpo de las system calls, creando una función con la correspondiente firma. Simplifica el proceso de agregar nuevas system calls, proveyendo una forma estandarizada de declararlas. Esta macro simplifica el proceso de crear system calls, manejando varios requerimientos de código boilerplate, como el pasaje de parámetros. Utilizar esta macro permite que los metadatos sobre la nueva system call estén disponibles para otras herramientas.
Se utiliza de la forma `SYSCALL_DEFINEn()`, donde n es el número de argumentos que recibirá la system call. La macro recibe como argumento el nombre de la system call seguido por los parámetros de la misma como argumentos (indicando tipo y nombre).

**- ¿Para que se utilizan la macros for_each_process y for_each_thread?**

La macro `for_each_process` permite iterar sobre la lista completa de tasks de la task struct (implementada como circular doubly-linked list).
La macro `for_each_thread` itera sobre todos los threads de una task dada.

**- ¿Para que se utiliza la función copy_to_user?**

Permite copiar datos desde el espacio del kernel al espacio de usuario. El kernel utiliza funciones como esta como API para tener acceso al espacio de direcciones del proceso de usuario que invocó la system call.

**- ¿Para qué se utiliza la función printk?, ¿porque no la típica printf?**

`printk` es una función utilizada para registrar mensajes en el log del kernel (informativos, warnings, errors), mientras que `printf` es la función de la librería estándar de C para realizar impresiones directamente en la salida estándar.

**- ¿Podría explicar que hacen las system call que hemos incluido?**

Se definen 3 system calls:

> `my_sys_call`

Registra un log del kernel imprimiendo el argumento que se le pasó.

> `get_task_info`

Registra un log del kernel con el PID y nombre de las tasks presentes en la lista de task structs. Define un tamaño máximo de 1024 caracteres mediante un buffer e imprime mientras no se sobrepase ese tamaño.
Luego, copia los strings de dichas impresiones a un buffer en el espacio de direcciones del proceso usuario que invocó la system call.

> `get_threads_info`

Guarda en un buffer del kernel reservado en memoria dinámica información de los procesos (nombre y PID de las tasks en la lista) y los hilos de cada proceso (nombre e ID). Luego, copia la información de este buffer al espacio de direcciones del proceso usuario que invocó la system call.

**Notas:**
A continuación, se añade el código objeto al Makefile en `kernel/` para indicar que debe ser compilado.

Luego, se agregan las system calls creadas como entradas en la syscall table de la arquitectura deseada.

Seguido a esto, se deben agregar las definiciones en los headers del kernel para poder llevar el array con los punteros a los manejadores (esto se hace en `include/uapi/asm-generic/unistd.h`).

## Monitoreando System Calls

### 1. Ejecute el programa anteriormente compilado `$ ./get_threads_info`. ¿Cual es el output del programa?

```
Información de los hilos en ejecución:
----------------------------------------
PID: 1 | Nombre: systemd | Estado: 1 
PID: 2 | Nombre: kthreadd | Estado: 1 
PID: 3 | Nombre: pool_workqueue_ | Estado: 1 
PID: 4 | Nombre: kworker/R-rcu_g | Estado: 8 
PID: 5 | Nombre: kworker/R-sync_ | Estado: 8 
PID: 6 | Nombre: kworker/R-slub_ | Estado: 8 
PID: 7 | Nombre: kworker/R-netns | Estado: 8 
PID: 9 | Nombre: kworker/0:0H | Estado: 8 
PID: 10 | Nombre: kworker/0:1 | Estado: 8 
PID: 11 | Nombre: kworker/u16:0 | Estado: 8 
PID: 12 | Nombre: kworker/R-mm_pe | Estado: 8 
PID: 13 | Nombre: rcu_tasks_kthre | Estado: 8 
PID: 14 | Nombre: rcu_tasks_rude_ | Estado: 8 
PID: 15 | Nombre: rcu_tasks_trace | Estado: 8 
PID: 16 | Nombre: ksoftirqd/0 | Estado: 1 
PID: 17 | Nombre: rcu_preempt | Estado: 8 
PID: 18 | Nombre: rcu_exp_par_gp_ | Estado: 1 
PID: 19 | Nombre: rcu_exp_gp_kthr | Estado: 1 
PID: 20 | Nombre: migration/0 | Estado: 1 
PID: 21 | Nombre: idle_inject/0 | Estado: 1 
PID: 22 | Nombre: cpuhp/0 | Estado: 1 
PID: 23 | Nombre: cpuhp/1 | Estado: 1 
PID: 24 | Nombre: idle_inject/1 | Estado: 1 
PID: 2
----------------------------------------
```

### 2. Luego de ejecutar el programa ahora ejecute `$ sudo dmesg`. ¿Cuál es el output? ¿por qué? (recuerde printk y lea el man de dmesg).

La salida del comando `dmesg` lista los nombres y PID de los procesos de forma similar a la salida del programa. Esto se debe a que la system call utilizaba `printk` para imprimir dicha información en los logs del kernel.
Si se revisa la entrada de `dmesg` en el manual, se indica que este comando es usado para imprimir o controlar el buffer del kernel. Por defecto, muestra todos los mensajes de dicho buffer.

### 3. Ejecute el programa anteriormente compilado con la herramienta strace `$ strace ./get_threads_info`. Aclaración: Si el programa strace no está instalado, puede instalarlo en distribuciones basadas en Debian con: `$ sudo apt-get install strace`

**En alguna parte del log de strace debería ver algo similar a lo siguiente:**

`syscall_0x1d4(0xffffed6696a8, 0x400, 0xaaaaadbf08b4, 0xffffb7fcb140, 0x93bb0cc6fda41a0e, 0xffffb7f71e78) = 0x400`

**Si luego ejecuto** `# echo $((0x1d4))`, **¿qué valor obtengo? ¿por qué?**

Se invocó `strace` sobre la ejecución del programa y dicha línea estaba presente. Si se imprime con `echo $((0x1d4))` se obtiene el valor 468, que corresponde al número de la system call `get_task_info` (encargada de obtener la información de los procesos).

`0x1d4` corresponde al número 468 en hexadecimal. Las system call desconocidas para strace son impresas en un formato en crudo, con el número de la system call en hexadecimal y el prefijo `syscall_`.

Los demás valores corresponden a los argumentos pasados a la system call y el valor de retorno, expresados de forma cruda como hexadecimales. Algunos de ellos corresponden a direcciones de memoria por ser punteros, otros son valores (como `0x400` = 1024).

___

## Módulos y Drivers

## Conceptos generales

### 1. ¿Cómo se denomina en GNU/Linux a la porción de código que se agrega al kernel en tiempo de ejecución? ¿Es necesario reiniciar el sistema al cargarlo? Si no se pudiera utilizar esto. ¿Cómo deberíamos hacer para proveer la misma funcionalidad en Gnu/Linux?

Los módulos son conocidos como las porciones de código que pueden ser cargados y descargados bajo demanda, en tiempo de ejecución. Por esto mismo, no es necesario recompilar el kernel ni reiniciar el sistema.
Sin ellos, el kernel de GNU/Linux sería totalmente monolítico. Se debería integrar el código directamente al kernel y recompilarlo para extender su funcionalidad.

### 2. ¿Qué es un driver? ¿Para qué se utiliza?

Un driver es componente de software que le permite al kernel comunicarse con dispositivos, permitiendo operar con ellos sin necesidad de conocer los detalles específicos del hardware de cada uno.
El driver es la capa intermedia entre el hardware del dispositivo y el kernel. Actúan una API para comunicar este último con el dispositivo, implementando el código específico para operar con este.

### 3. ¿Por qué es necesario escribir drivers?

Es necesario escribir drivers para abstraer al kernel de los detalles del hardware de cada dispositivo, incrementando la interoperabilidad del sistema. Si se quiere comunicar con un dispositivo específico, basta con descargar el driver que permite dicha comunicación. El kernel invocará funciones expuestas por el driver y este ejecutará el código específico para el dispositivo.

### 4. ¿Cuál es la relación entre módulo y driver en GNU/Linux?

Un driver permite la comunicación con dispositivos de hardware mientras que un módulo es una porción de código que puede ser cargada y descargada dinámicamente. Los drivers **pueden** ser implementados como módulos, permitiendo su carga dinámica. Esto es especialmente útil con dispositivos que podrían ser añadidos o removidos del sistema. Además, los módulos del kernel permiten agregar nuevas características y solucionar errores en drivers existentes sin que esto impacte en la totalidad del kernel.

Sin embargo, hay que tener en cuenta que no todos los módulos del kernel son drivers. También existen módulos que podrían proveer servicios o extender la funcionalidad del kernel, más allá de comunicarse con un dispositivo externo.
Además, los drivers no necesariamente se implementan como módulos. Aunque esto los dota de mayor flexibilidad, también podrían ser parte del código principal del kernel.

### 5. ¿Qué implicancias puede tener un bug en un driver o módulo?

Dado que los drivers y módulos se ejecutan en modo kernel, tienen permisos ilimitados y acceso irrestricto al hardware. Por lo tanto, un bug en ellos puede producir errores graves que conduzcan a un kernel panic y afecten a todo el sistema.

### 6. ¿Qué tipos de drivers existen en GNU/Linux?

Se pueden clasificar en tres tipos:

- Character Device Drivers: drivers utilizados para dispositivos que transfieren información de forma secuencial, byte a byte. Por ejemplo: teclado, puerto serial.

- Block Device Drivers: utilizados para dispositivos que transfieren datos en bloques de tamaño fijo, tal como HDDs o SSDs.

- Network Device Drivers: estos drivers son responsables de gestionar interfaces de red, tal como adaptadores Wi-Fi o Ethernet.

### 7. ¿Qué hay en el directorio /dev? ¿Qué tipos de archivo encontramos en esa ubicación?

En el directorio `/dev` se encuentras los device nodes, que son archivos especiales que representan dispositivos de hardware. Cuando un driver es cargado, generalmente se crea su correspondiente device node. Los procesos pueden interactuar con los dispositivos leyendo o escribiendo estos archivos.

### 8. ¿Para qué sirven el archivo /lib/modules/<version>/modules.dep utilizado por el comando modprobe?

`modules.dep` es un archivo de texto generado por el comando `depmod` que lista las dependencias de cada módulo. Es un texto informativo legible para humanos. Su contraparte funcional es el binario `modules.dep.bin`. Es utilizado por herramientas como `modprobe`, que permite cargar y descargar módulos.

Las dependencias de un módulo le permiten funcionar y deben ser respetadas al momento de cargarlo o descargarlo. Por ello, este archivo resulta de gran utilidad.

### 9. ¿En qué momento/s se genera o actualiza un initramfs?

El `initramfs` es un sistema de archivos temporal que se monta en memoria durante el arranque del sistema. Este contiene ejecutables, drivers y módulos necesarios para lograr iniciar el sistema.

- Este es generado durante el proceso de booteo del sistema, permitiendole al kernel acceder a los archivos y drivers necesarios.
- Cuando un nuevo kernel es instalado, se ejecuta el comando `update-initramfs`, lo que genera o actualiza el mismo. Este comando garantiza que el `initramfs` contiene los drivers y archivos necesarios para que el nuevo kernel bootee correctamente.
- En caso de realizar cambios en lo drivers, como agregar nuevos, el archivo `initramfs` también debe ser actualizado.
- Si se realizan cambios en el file system, tal como activar RAID o encriptación, podría ser necesario actualizarlo.

### 10. ¿Qué módulos y drivers deberá tener un initramfs mínimamente para cumplir su objetivo?

El `initramfs` tiene como objetivo permitir que se monte el sistema de archivos raíz real durante el arranque. Por ello, requiere módulos y drivers indispensables para acceder a este.

Debe tener:

- Driver para dispostivo de almacenamiento: el kernel necesitará acceder al dispositivo de almacenamiento donde está `/`.
- Driver del file system: el kernel debe poder interpretar el formato del root filesystem. Ej.: `ext4`, `btrfs`.

Luego, podría ser necesario incluir soporte para particiones, LVM o dispositivos de red, según sea el caso.
El archivo `initramfs` debería incluir todos los drivers específico para cualquier dispositivo de hardware del sistema, ya que el kernel necesitará cargar los módulos necesarios.

## Práctica guiada

#### Desarrollando un módulo simple para Linux

### 1. Crear el archivo memory.c con el siguiente código (puede estar en cualquier directorio, incluso fuera del directorio del kernel):

```c
#include <linux/module.h>
MODULE_LICENSE("Dual BSD/GPL");
```

### 2. Crear el archivo Makefile con el siguiente contenido: `obj-m := memory.o`

**Responda lo siguiente:**

**a. Explique brevemente cual es la utilidad del archivo Makefile.**

Como se vió en la anterior práctica, los archivos makefile son archivos de configuración que le indican cómo compilar el código fuente. Su principal utilidad es automatizar procesos de compilación, determinando que archivos han cambiado y generando los binarios necesarios.

En este caso concreto, el contenido `obj-m := memory.o` declara el archivo objeto que el sistema de construcción del kernel debe procesar. 
La sintaxis `obj-m` indica explícitamente que el archivo resultante debe ser un módulo cargable (`.ko`) y no un componente estático integrado en el binario del kernel.
Establece que el archivo de salida `memory.o` (y luego `memory.ko`) debe construirse a partir del código fuente que comparta el mismo nombre (`memory.c`). Esto lo hace de forma automática.
Este archivo será leíod por el Makefile principal del kernel cuando se realice la compilación.

**b. ¿Para qué sirve la macro MODULE_LICENSE? ¿Es obligatoria?**

La macro informa al kernel bajo que licencia se distribuye el modelo. Esto es importante pues el kernel de Linux es GPL, y ciertas funciones internas solo están disponibles para módulos con licencias compatibles. No es obligatoria en el sentido de que el módulo puede compilarse sin ella, pero se emitirá un mensaje indicando que "kernel tainted", lo que implica que el muchas funciones del kernel podrían no estar disponibles para el módulo.

### 3. Ahora es necesario compilar nuestro módulo usando el mismo kernel en que correrá el mismo, utilizaremos el que instalamos en el primer paso del ejercicio guiado.

`$ make -C <KERNEL_CODE> M=$(pwd) modules`

**Responda lo siguiente:**

**a. ¿Cuál es la salida del comando anterior?**

```
make: se entra en el directorio '/home/so/kernel/linux-6.13'
make[1]: se entra en el directorio '/home/so/practica2/module'
  CC [M]  memory.o
  MODPOST Module.symvers
  CC [M]  memory.mod.o
  CC [M]  .module-common.o
  LD [M]  memory.ko
make[1]: se sale del directorio '/home/so/practica2/module'
make: se sale del directorio '/home/so/kernel/linux-6.13'
```

**b. ¿Qué tipos de archivo se generan? Explique para qué sirve cada uno.**

Se generaron los siguientes archivos:

- `memory.o`: archivo objeto
- `memory.mod.c` y `memory.mod.o`: archivos temporales generados por el proceso modpost que contienen metadatos del módulo.
- `memory.ko`: binario final. Contiene código ejecutable y la información necesaria para que el kernel lo inserte en su espacio de memoria.
- `Module.symvers` y `modules.order`: archivos de control que listan el orden de los módulos y los símbolos exportados (por el kernel y otros módulos).

**nota:**

Se entiende por símbolo a un nombre que representa una dirección de memoria donde reside algo útil, como una función o variable global.

Cuando se compila el kernel, se genera una tabla de símbolos. Si una función dentro del kernel se llama `printk`, el símbolo es el nombre `printk`. La tabla de símbolos mapea el nombre a la dirección de memoria.

Para que un módulo use símbolos (funciones o variables), el kernel debe exportarlas.

**c. Con lo visto en la Práctica 1 sobre Makefiles, construya un Makefile de manera que si ejecuto**

*i. make, nuestro módulo se compila*

*ii. make clean, limpia el módulo y el código objeto generado*

*iii. make run, ejecuta el programa*

### 4. El paso que resta es agregar y eventualmente quitar nuestro módulo al kernel en tiempo de ejecución.

**Ejecutamos:**

`# insmod memory.ko`

**Responda lo siguiente:**

**¿Para qué sirven el comando insmod y el comando modprobe? ¿En qué se diferencian?**

- `insmod`: es una herramienta de bajo nivel para cargar módulos. Carga el archivo `.ko` específico que se le indique. No resuelve dependencias, por lo que si el módulo necesita de otro para funcionar, el comando fallará.

- `modprobe`: herramienta inteligente para cargar y descargar módulos del kernel. Busca el módulo en el directorio estándar y carga automáticamente todas las dependencias necesarias antes de cargar el módulo solicitado.

### 5. Ahora ejecutamos:

`$ lsmod | grep memory`

**Responda lo siguiente:**

**a. ¿Cuál es la salida del comando? Explique cuál es la utilidad del comando lsmod.**

```
$ lsmod | grep memory

memory                  8192  0
```

El comando lsmod lista los módulos que están actualmente cargados en la memoria del kernel. Lee la información formateada de `/proc/modules`. Su salida muestra el nombre del módulo, su tamaño en memoria y si está siendo usado por otros módulos.

**b. ¿Qué información encuentra en el archivo /proc/modules?**

Este archivo contiene una lista de todos los módulos cargados en el kernel. 

- La primera columna contiene el nombre del módulo. 
- La segunda su tamaño en memoria.
- La tercera cuantas instancias del módulo están actualmente cargadas.
- La cuarta indica si el módulo tiene alguna dependencia (y los lista). 
- La quinta columna lista en que estado de carga está el módulo (Live, Loading, Unloading).
- La sexta columna lista el offset del módulo cargado en la memoria del kernel.

**c. Si ejecutamos more /proc/modules encontramos los siguientes fragmentos ¿Qué información obtenemos de aquí?:**

```
memory 8192 0 - Live 0x0000000000000000 (OE)
binfmt_misc 24576 1 - Live 0x0000000000000000
intel_rapl_msr 16384 0 - Live 0x0000000000000000
intel_rapl_common 32768 1 intel_rapl_msr, Live 0x0000000000000000
```

La información obtenida por cada columna es la descrita en el punto anterior. `OE` implica Out-of-tree (compilado fuera del árbol oficial) y Externally Linked.

**d. ¿Con qué comando descargamos el módulo de la memoria?**

Se puede realizar con `rmmod` o `modprobe -r`. Ej.: `rmmod memory`.

### 6. Descargue el módulo memory. Para corroborar que efectivamente el mismo ha sido eliminado del kernel ejecute el siguiente comando:

`lsmod | grep memory`

### 7. Modifique el archivo memory.c de la siguiente manera (código no adjuntado)

**a. Compile y cargue en memoria el módulo.**

**b. Invoque al comando dmesg**

**c. Descargue el módulo de memoria y vuelva a invocar a dmesg**

### 8. Responda lo siguiente:

**a. ¿Para qué sirven las funciones module_init y module_exit?. ¿Cómo haría para ver la información del log que arrojan las mismas?.**

- `module_init()`: define la función que se ejecuta al cargar el módulo (prepara estructuras, reserva memoria).

- `module_exit()`: define la función que se ejecuta al descargar el módulo (libera recursos).

Se utiliza el comando `dmesg` para imprimir los mensajes en el buffer del kernel.

**b. Hasta aquí hemos desarrollado, compilado, cargado y descargado un módulo en nuestro kernel. En este punto y sin mirar lo que sigue. ¿Qué nos falta para tener un driver completo?.**

Un driver es una porción de código que traduce las órdenes del kernel hacia un dispositivo del cual el kernel no posee detalles de su implementación.

Por lo tanto, faltaría registrarse como un dispositivo en `/dev/`, definir operaciones (determinar que sucede cuando se lo lee o escribe) e indicar su major and minor number.

**c. Clasifique los tipos de dispositivos en Linux. Explique las características de cada uno.**

Los dispositivos de hardware se pueden clasificar en dispositivos de acceso aleatorio (ej.: discos) y dispositivos seriales (ej.: mouse). En base a esto, podemos clasificar los drivers tal como se indica en el inciso 6 de los conceptos generales.

#### Desarrollando un Driver

### 1. Modifique el archivo memory.c para que tenga el siguiente código: https://gitlab.com/unlp-so/codigo-para-practicas/-/blob/main/practica2/crear_driver/1_memory.c


### 2. Responda lo siguiente:

**a. ¿Para qué sirve la estructura ssize_t y memory_fops? ¿Y las funciones register_chrdev y unregister_chrdev?**

> `ssize_t`

Es un tipo de dato definido por el estándar POSIX que representa un tamaño con signo. Se utiliza como valor de retorno en funciones de lectura/escritura porque estas deben poder devolver un número positivo (cantidad de bytes procesados), el valor 0 (EOF) o un valor negativo (código de error).

> `memory_fops`

Esta estructura (del tipo struct file_operations) determina como el kernel interactúa con el dispositivo, definiendo operaciones como abrir, leer, escribir o cerrar el dispositivo.

Actúa como tabla de punteros de las funciones que el kernel puede invocar sobre un dispositivo. Cuando una aplicación en el espacio de usuario llama a operaciones como open() o read() sobre el archivo del dispositivo, el kernel busca la correspondiente función en la estructura del dispositivo y la ejecuta.

Algunas de las operaciones más comunes definidas en esta estructura son open, release, read, write.

> `register_chrdev`

Esta función registra un dispositivo de caracteres en el kernel. Reserva un major number y lo vincula con un nombre (en este caso "memory") y su tabla de operaciones ("memory_fops" en este caso).

> `unregister_chrdev`

Libera el major number y le indica al kernel que ya no debe gestionar peticiones para ese dispositivo. Operación contraria a `register_chrdev`

**b. ¿Cómo sabe el kernel que funciones del driver invocar para leer y escribir al dispositivo?**

Como se explicó antes, el kernel conoce las funciones a invocar ante cada operación debido a la estructura file_operations. Cuando se registra el dispositivo con `register_chrdev` se le pasa la dirección de `memory_fops`. Dentro de esa estructura se mapean las operaciones a las funciones que la atienden.

**c. ¿Cómo se accede desde el espacio de usuario a los dispositivos en Linux?**

Dado que en Linux todo es un archivo, los dispositivos también lo son. Se acceden mediante archivos ubicados en `/dev`.

Para acceder a un driver específico, se debe crear un device node manualmente con el comando `mknod` o de forma automática con `udev`. Una vez creado, puede ser accedido con comandos comunes como `cat` o `echo`. 

**d. ¿Cómo se asocia el módulo que implementa nuestro driver con el dispositivo?**

La asociación se realiza mediante el major number, indicado en `register_chrdev`. Este número se asocia al archivo del dispositivo. Cuando un programa abre el archivo, el kernel verifica el major number del mismo, busca su tabla interna para mapear el dispositivo con el módulo y utiliza la estructura `memory_fops` para determinar como resolver las operaciones.

También puede apoyarse en el minor number para desambigüar entre dispositivos que comparten major number (por ejemplo, distintas particiones de un mismo disco).

Con ambos, cada dispositivo de identifica de manera unívoca.

**e. ¿Qué hacen las funciones copy_to_user y copy_from_user? (https://developer.ibm.com/technologies/linux/articles/l-kernel-memory-access/).**

`copy_to_user(dest, src, n)` copia n bytes desde la memoria del kernel hacia la memoria de la aplicación de usuario. Se usa en la operación de lectura.

`copy_from_user(dest, src, n)` copia n bytes desde la aplicación de usuario hacia la memoria del kernel. Se usa en la operación de escritura.

El espacio de memoria del kernel y el espacio de memoria de usuario están aislados por cuestiones de seguridad. Para intercambiar datos entre ambos, no es posible desreferenciar punteros del espacio de usuario. En vez de eso, se utilizan estas funciones para realizar el proceso de manera segura.

A diferencia de otros mecanismos, estas funciones verifican si el acceso es seguro y legal antes de mover los datos, evitando que un error en una aplicación de usuario provoque un kernel panic.

### 3. Ahora ejecutamos lo siguiente: `# mknod /dev/memory c 60 0`


### 4. Y luego: `# insmod memory.ko`

**Responda lo siguiente:**

*i. ¿Para qué sirve el comando mknod? ¿qué especifican cada uno de sus parámetros?.*

El comando `mknod` permite crear archivos especiales de bloques o caracteres. 

Se utiliza de la siguiente forma:

`mknod <device-file> <device-type> <major-number> <minor-number>`, donde:

- `device-file` es la ruta del archivo en `/dev` 
- `device-type` es el tipo de dispositivo (de caracteres o bloques) 
- `major-number` y `minor number` refieren a los números identificadores del dispositivo.

*ii. ¿Qué son el “major” y el “minor” number? ¿Qué referencian cada uno?*

Major number es un identificador único asignado a un driver de un dispositivo en el kernel. Cada dispositivo tiene un major number distinto, que es usado por el kernel para diferenciarlo y determinar que driver utilizar cuando el dispositivo es accedido. El major number también es conocido como device number o device major. 

Minor number es un identificador menor utilizado con el major number para identificar unívocamente un dispositivo específico dentro de una clase de dispositivos, como pueden ser diferentes particiones dentro de un disco duro.

El major number puede identificar a un driver particular o a un tipo de dispositivo (por ejemplo, un major number para todos los dispositivos de bloques). Para este segundo caso, el minor number permite distinguir entre las diferentes instancias de un tipo de dispositivo.

### 5. Ahora escribimos a nuestro dispositivo:

`echo -n abcdef > /dev/memory`

### 6. Ahora leemos desde nuestro dispositivo:

`more /dev/memory`

### 7. Responda lo siguiente:

**a. ¿Qué salida tiene el anterior comando?, ¿Porque? (ayuda: siga la ejecución de las funciones memory_read y memory_write y verifique con dmesg)**

El comando solo muestra el último caracter ingresado ("f").

Al ejecutar `echo -n abcdef > /dev/memory` se realiza un write() que invoca a `memory_write`. Si se analiza el código, este recibe, entre otras cosas, el puntero al buffer y la cantidad de caracteres. En la variable tmp se guarda el último caracter (porque a buf se le suma un offset de 5) y al llamar a `copy_from_user` se envía al buffer del kernel 1 byte partiendo desde este último caracter. En resumen, se escribe un solo caracter.
Notar también que no se manipula la variable f_pos, por lo que cada escritura sobreescribe a la anterior (no se mueve el cursor). De todos modos, como siempre se está apuntando a f, lo único que sucede es que se escribe 6 veces f.

Al ejecutar `more /dev/memory` se lee el dispositvo y se llama a `memory_read`. Al llamar a `copy_to_user`, se copia al espacio de usuario únicamente 1 byte, siendo este el caracter "f".

Con `dmesg` se pueden ver los 6 llamados a `memory_write` y dos `memory_read` (esto sucede por la gestión que realizan las utilidades de la shell).

**nota:**

Como prueba, se cambió `tmp=buf+count-1` por `tmp=buf`. Si se mantiene el valor del return en 1, el caracter que queda en el buffer es f. Esto sucede porque envía "abcdef" con 6 pero recibe 1, entonces vuelve a llamarse a write con "bcdef" con 5. Así sucesivamente hasta que se llama a write con "f" y 1. Al retornar 1, se considera finalizada la escritura (buf es un puntero).

Si se realiza dicho cambio y además se cambia `return 1` por `return count`, entonces el caracter que queda es "a", ya que se escribe solo un byte y se retorna lo que el comando echo espera (envió una cadena de longitud 6 y recibe un 6 como retorno).

**b. ¿Cuántas invocaciones a memory_write se realizaron?**

Se realizaron 6 invocaciones a `memory_write`. Esto se debe a que se recibe una cadena `abcdef` con un count de 6, pero el return del `memory_write` es de 1. `echo` espera recibir un 6, que es la longitud de la cadena que envió. Como no lo recibe, considera que la escritura fue parcial. Entonces, desplaza su puntero de lectura y vuelve a llamar a la system call write. Esto se repite 6 veces.

**c. ¿Cuál es el efecto del comando anterior? ¿Por qué?**

El comando `more` permite visualizar el contenido de un archivo en la terminal. Este invoca a la system call `read`, lo que implica una llamada a `memory_read.` Aquí se copia un elemento del buffer al espacio del usuario. Luego de esto, incrementa la variable f_pos si esta se encuentra en 0. Esta actúa como un control manual del puntero de la posición del archivo.

Si no se incrementara f_pos o no se devolviera 0 en algún momento (indica EOF), los comandos de lectura como more o cat entrarían en bucle infinito, pues siempre creerían que hay datos disponibles por leer.

El código en cuestión funciona de la siguiente manera:

```c
if (*f_pos == 0) {
    *f_pos += 1;
    return 1; // Indica que leyó 1 byte exitosamente
} else {
    return 0; // Indica EOF (End of File)
}
```

**e. Hasta aquí hemos desarrollado un ejemplo de un driver muy simple pero de manera completa, en nuestro caso hemos escrito y leído desde un dispositivo que en este caso es la propia memoria de nuestro equipo. En el caso de un driver que lee un dispositivo como puede ser un file system, un dispositivo usb,etc. ¿Qué otros aspectos deberíamos considerar que aquí hemos omitido? ayuda: semáforos, ioctl, inb, outb.**

Desarrollar un driver para un dispositivo físico requiere manejar asincronía y la complejidad de su hardware. Algunas cuestiones que deben tenerse en cuenta:

- Concurrencia/sincronización: el driver actual no es reentrante. Si dos procesos escriben a la vez en `/dev/memory` se produciría una condición de carrera. En hardware real, se deben utilizar operaciones de exclusión mutua para manejar la concurrencia.

- Gestión I/O (inb/outb): en el driver se usó kmalloc para simular memoria. Un dispositivo real debe comunicarse con registros de hardware mediante funciones como `inb()` o `outb()` (en x86). Además, se deberían mapear las direcciones físicas del hardware en el espacio de direccionamiento virtual del kernel.

- Manejo de interrupciones: el hardware real no responde instantáneamente. Un driver debe registrar un manejador de interrupciones para que el hardware le avise a la CPU cuando los datos están listos.

- Operaciones de control (ioctl): la estructura `file_operations` aquí solo tiene read and write. Para hardware real se requiere la syscall ioctl (Input/Output Control), que permite enviar comandos específicos que no son simplemente flujo de datos.

- DMA: para dispositivos de alta velocidad (como discos o placas de red), el driver debe configurar el DMA para que el hardware transfiera datos directamente a la RAM sin que el CPU intervenga en cada byte.